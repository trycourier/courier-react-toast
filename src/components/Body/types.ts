export interface BodyWrapperProps {
  clickAction: string;
  onClick: Function;
  title: string;
  body: string;
  icon: string;
}

export interface BodyProps {
  title: string;
  body: string;
  icon: string;
}