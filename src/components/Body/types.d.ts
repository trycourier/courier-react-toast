export interface WrapperProps {
  clickAction: string;
  onClick: () => {};
  title: string;
  body: string;
  icon: string;
}

export interface BodyProps {
  title: string;
  body: string;
  icon: string;
}